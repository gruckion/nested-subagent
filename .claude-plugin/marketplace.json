{
  "name": "fallback-agent",
  "owner": {
    "name": "gruckion"
  },
  "metadata": {
    "description": "Fallback agent with full tool access for unlimited nesting"
  },
  "plugins": [
    {
      "name": "fallback",
      "source": "./",
      "description": "Fallback agent that spawns fresh Claude processes with full tool access. Bypasses the native Task tool's recursion limit, enabling unlimited nesting.",
      "version": "1.0.3",
      "mcpServers": {
        "fallback": {
          "command": "node",
          "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/dist/index.mjs"]
        }
      },
      "strict": false
    }
  ]
}
